<h1><%= @list.name %></h1>

<p>The list <%= @list.name %> has <%= @list.movies.length %> movie(s).</p>

<ul>
  <% @list.movies.each do |movie| %>
    <li><%= movie.title %></li>
  <% end %>
</ul>

<%= link_to "Add a movie to this list", new_list_bookmark_path(@list) %>
