<h1>All my lists 🍿</h1>

<div>
  <% @lists.each do |list| %>
    <div><%= link_to list.name, list_path(list) %></div>
  <% end %>
</div>

<div class="mt-5">
  <%= link_to "New list", new_list_path %>
</div>
